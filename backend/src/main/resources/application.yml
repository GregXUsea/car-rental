server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_rental?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 926565
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: CarRentalSystemSecretKey2026VeryLongSecretKeyForHS256Algorithm
  expiration: 86400000

openai:
  api-key: ${OPENAI_API_KEY:sk-06b67a5a9e384b5d9b36fc36a80f7087}
  base-url: ${OPENAI_BASE_URL:https://api.deepseek.com/v1}
  model: ${OPENAI_MODEL:deepseek-chat}

# 邮箱SMTP配置（QQ邮箱示例）
# 第1步：登录 mail.qq.com → 设置 → 账户 → 开启POP3/SMTP服务 → 获取授权码
# 第2步：把下面的 your-email@qq.com 改成你的QQ邮箱，授权码改成你刚获取的
mail:
  host: smtp.qq.com
  port: 465
  username: 3534313902@qq.com
  password: kwouspzaypsncidc
  from: 3534313902@qq.com

logging:
  level:
    com.carrental: debug
