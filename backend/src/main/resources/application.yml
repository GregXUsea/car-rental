server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_rental?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 926565
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: CarRentalSystemSecretKey2026VeryLongSecretKeyForHS256Algorithm
  expiration: 86400000

spark:
  api-password: ${SPARK_API_PASSWORD:}
  base-url: ${SPARK_BASE_URL:https://spark-api-open.xf-yun.com/agent/v1}
  model: ${SPARK_MODEL:spark-x}

logging:
  level:
    com.carrental: debug

spring.servlet.multipart:
  max-file-size: 5MB
  max-request-size: 10MB

file:
  upload-dir: ${user.dir}/uploads
