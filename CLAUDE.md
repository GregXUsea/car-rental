# CLAUDE.md

## Git 提交规范

### 强制要求：每完成一部分功能代码后必须提交

- 每完成一个功能模块或子功能后，**必须**执行 `git add` + `git commit`，将变更提交到本地仓库
- 每完成一个阶段的工作后，**必须**执行 `git push` 推送到远程仓库（GitHub/Gitee）
- 保持提交粒度适中：一个功能点一个 commit，不要攒很多功能一起提交

### 提交记录规范（重要：教师检查提交记录）

- **严禁**在 commit message 中出现 "Co-authored-by: AI"、"AI assisted"、"Generated by Claude" 等任何 AI 辅助标记
- **严禁**使用 `--signoff` 或 `--trailer` 添加 AI 相关的 commit trailer
- commit message 使用**中文**，格式为：`功能描述 — 补充说明`
- 示例：
  ```
  新增：用户登录接口 — 支持账号密码登录，返回JWT令牌
  修复：车辆状态判断逻辑 — 已预约车辆不可重复预约
  优化：AI选车助手推荐逻辑 — 提高匹配准确率
  重构：订单模块 — 统一在租/已完成状态枚举
  ```

### Git 配置检查

- 提交前确认 `git config user.name` 和 `git config user.email` 为个人真实信息（非 AI 账号）
- 当前仓库已绑定 GitHub 远程仓库，推送前确认分支为 `main`

## 团队协作

### 成员与分工

| 成员 | 开发环境 | 负责模块 | 具体功能 |
|------|----------|----------|----------|
| GregXUsea | VS Code | 用户模块 + AI 模块 | 登录、注册、修改密码、找回密码、个人信息管理、AI 智能选车助手 |
| superxiaohang | IntelliJ IDEA | 租车业务 + 订单模块 | 车辆展示、租赁汽车、归还车辆、预约租车、费用支付、车辆检索、租车记录查询、订单中心 |

- 按**功能模块**分工（非前后台划分），每人工作量占比约 **50%**
- 组内分工需在课程设计报告中按上表明确写明
- GregXUsea 偏向前端交互 + AI 集成，superxiaohang 偏向核心业务逻辑

### IDE 兼容注意事项

- `.idea/` 已在 `.gitignore` 中排除（IntelliJ IDEA 配置）
- `.vscode/` 已跟踪（VS Code 配置），IntelliJ 用户不要删除此目录
- 不要提交个人 IDE 的本地配置（如 workspace.xml、运行配置等）
- `backend/pom.xml` 是项目公共配置，修改前双方沟通确认
- 依赖变更（pom.xml / package.json）需及时同步给对方

### 协作 Git 规范

- **每人用自己的 GitHub 账号提交**，教师通过提交记录核查工作量
- 提交前确认 `git config user.name` 和 `git config user.email` 为本人信息
- **严禁**在 commit message 中出现 "Co-authored-by: AI" 等 AI 标记
- 每次提交聚焦单一功能点，commit message 用中文清晰描述
- 拉取代码前先 `git stash` 暂存本地改动，避免冲突

## 项目结构

```
car-rental-system/
├── backend/          # SpringBoot 后端
│   ├── src/main/java/com/carrental/
│   │   ├── config/       # 配置类
│   │   ├── controller/   # 控制器
│   │   ├── service/      # 业务逻辑层
│   │   ├── mapper/       # MyBatis/MyBatis-Plus 数据访问层
│   │   ├── entity/       # 实体类
│   │   └── common/       # 公共工具类
│   └── src/main/resources/
│       └── application.yml
├── frontend/         # Vue 前端
├── docs/             # 文档
│   └── requirements.md  # 需求规格文档（开发唯一依据）
├── init.sql          # 数据库初始化脚本
└── tools/            # 工具脚本
```

## 开发约定

### 需求依据

- 所有功能开发以 `docs/requirements.md` 为唯一需求文档
- 需求变更需先更新 `requirements.md` 再开发

### 代码规范

- 后端：Java 类名驼峰、方法名驼峰、常量全大写下划线分隔
- 前端：Vue 组件名 PascalCase，文件名 kebab-case
- 注释：关键业务逻辑必须写中文注释

### 功能验收

- 每完成一个功能模块，对照 `requirements.md` 第七章 checklist 打勾确认
- 确保运行通过后再提交

### AI 辅助声明（仅限课程设计报告）

- 代码本身不需要标注 AI 辅助
- 课程设计报告中使用 AI 辅助撰写的章节，需在章节末尾标注：
  > 本节初稿由 AI 辅助生成，已由本人结合实际修改完善
